# Техническое задание: Earnings Tracker Bot

**Версия:** 1.0  
**Дата создания:** 13.01.2026  
**Статус:** 📋 Утверждено

---

## 1. Описание проекта

**Earnings Tracker Bot** — Telegram-бот для автоматизированного учёта смен и расчёта заработка контрагентов (фрилансеров, подрядчиков) в киноиндустрии.

### Ключевые возможности:
- ✅ Ввод данных о сменах на естественном языке через чат
- 🤖 AI-парсинг сообщений для извлечения времени, даты и услуг
- 💰 Автоматический расчёт заработка с учётом переработок, прогрессивных ставок
- 📊 Статистика по проектам и периодам
- 📱 Telegram Mini App для настройки проектов

---

## 2. Технический стек

### Backend:
- **Язык:** Python 3.10+
- **Telegram библиотека:** aiogram 3.x
- **База данных:** SQLite (файл `data.db`)
- **AI парсинг:** OpenAI API (модель GPT-4.1-nano)

### Frontend (Mini App):
- **Технологии:** HTML5, CSS3, JavaScript (Vanilla)
- **API:** Telegram Web App API

### Инфраструктура:
- **Хостинг:** Облачный сервер пользователя
- **Деплой:** Ручной (через Git/SFTP)

---

## 3. Архитектура системы

```
┌─────────────────┐
│  Telegram User  │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────┐
│    Telegram Bot (aiogram)       │
│  ┌──────────────────────────┐   │
│  │  Command Handlers        │   │
│  │  - /start                │   │
│  │  - /new_project          │   │
│  │  - /stats                │   │
│  └──────────────────────────┘   │
│  ┌──────────────────────────┐   │
│  │  Message Parser (AI)     │   │
│  │  - OpenAI GPT-4.1-nano   │   │
│  └──────────────────────────┘   │
│  ┌──────────────────────────┐   │
│  │  Earnings Calculator     │   │
│  │  - Base pay              │   │
│  │  - Overtime (progressive)│   │
│  │  - Additional services   │   │
│  └──────────────────────────┘   │
└────────────┬────────────────────┘
             │
             ▼
      ┌─────────────┐
      │  SQLite DB  │
      │  (data.db)  │
      └─────────────┘
```

---

## 4. Структура базы данных

### 4.1. Таблица `users`
Хранит информацию о пользователях бота.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY | ID пользователя (Telegram ID) |
| username | TEXT | Username в Telegram |
| contractor_type | TEXT | "person" или "transport" |
| created_at | TIMESTAMP | Дата регистрации |
| is_active | BOOLEAN | Активен ли профиль |

### 4.2. Таблица `projects`
Хранит проекты пользователей.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY | ID проекта |
| user_id | INTEGER | FK → users.id |
| name | TEXT | Название проекта |
| description | TEXT | Описание |
| is_active | BOOLEAN | Активен ли проект |
| created_at | TIMESTAMP | Дата создания |

### 4.3. Таблица `professions`
Настройки профессии для проекта (только для типа "person").

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY | ID профессии |
| project_id | INTEGER | FK → projects.id |
| position | TEXT | Должность |
| base_rate_net | INTEGER | Базовая ставка (нетто, руб) |
| base_rate_gross | INTEGER | Базовая ставка (брутто, руб) |
| base_overtime_rate | INTEGER | Базовая ставка переработки (руб/ч) |
| daily_allowance | INTEGER | Суточные (руб) |
| base_shift_hours | DECIMAL | Базовое кол-во часов в смене |
| break_hours | DECIMAL | Часы разрыва между сменами |
| tax_percentage | DECIMAL | Налоговый процент (%) |
| payment_schedule | TEXT | "monthly" / "bi_monthly" / "project_end" |
| conditions | TEXT | Спец. условия (для AI) |
| created_at | TIMESTAMP | Дата создания |

### 4.4. Таблица `progressive_rates`
Прогрессивные ставки переработки.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY | ID ставки |
| profession_id | INTEGER | FK → professions.id |
| hours_from | DECIMAL | Часы от |
| hours_to | DECIMAL | Часы до (NULL = бесконечность) |
| rate | INTEGER | Ставка (руб/ч) |
| order | INTEGER | Порядок применения |

**Пример:**
```
| order | hours_from | hours_to | rate |
|-------|-----------|----------|------|
| 1     | 0         | 2        | 500  |
| 2     | 2         | 4        | 600  |
| 3     | 4         | NULL     | 700  |
```

### 4.5. Таблица `additional_services`
Дополнительные услуги.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY | ID услуги |
| profession_id | INTEGER | FK → professions.id |
| name | TEXT | Название услуги |
| cost | INTEGER | Стоимость (руб) |
| application_rule | TEXT | "every_shift" / "on_mention" / "with_service" |
| linked_service_id | INTEGER | FK → additional_services.id (для "with_service") |
| keywords | TEXT | Ключевые слова для парсинга (JSON array) |

### 4.6. Таблица `shifts`
Смены пользователя.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY | ID смены |
| project_id | INTEGER | FK → projects.id |
| date | DATE | Дата смены |
| start_time | TIME | Время начала |
| end_time | TIME | Время окончания |
| total_hours | DECIMAL | Всего часов |
| overtime_hours | DECIMAL | Часы переработки |
| is_expense_day | BOOLEAN | Включены ли суточные |
| status | TEXT | "draft" / "confirmed" / "calculated" |
| original_message | TEXT | Исходное сообщение от пользователя |
| parsed_data | TEXT | Распознанные данные (JSON) |
| created_at | TIMESTAMP | Дата создания |
| confirmed_at | TIMESTAMP | Дата подтверждения |

### 4.7. Таблица `shift_services`
Связь смен и дополнительных услуг.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY | ID связи |
| shift_id | INTEGER | FK → shifts.id |
| service_id | INTEGER | FK → additional_services.id |
| applied | BOOLEAN | Применена ли услуга |

### 4.8. Таблица `earnings`
Расчёт заработка по сменам.

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY | ID записи |
| shift_id | INTEGER | FK → shifts.id |
| base_pay_net | INTEGER | Базовая оплата (нетто, руб) |
| base_pay_gross | INTEGER | Базовая оплата (брутто, руб) |
| overtime_pay | INTEGER | Оплата переработок (руб) |
| daily_allowance | INTEGER | Суточные (руб) |
| services_pay | INTEGER | Оплата доп. услуг (руб) |
| total_net | INTEGER | Итого нетто (руб) |
| total_gross | INTEGER | Итого брутто (руб) |
| calculation_details | TEXT | Детали расчёта (JSON) |
| calculated_at | TIMESTAMP | Дата расчёта |

---

## 5. Пользовательские сценарии

### 5.1. Первый запуск (Онбординг)

**Актёр:** Новый пользователь

**Шаги:**
1. Пользователь отправляет `/start` боту
2. Бот приветствует и предлагает выбрать тип контрагента:
   ```
   👋 Добро пожаловать в Earnings Tracker!
   
   Я помогу вам вести учёт смен и автоматически рассчитывать заработок.
   
   Выберите тип контрагента:
   
   [👤 Я исполнитель (человек)]
   [🚗 Я владелец транспорта] (скоро)
   ```
3. Пользователь выбирает "👤 Я исполнитель"
4. Бот создаёт профиль типа "person" в БД
5. Бот предлагает создать первый проект:
   ```
   ✅ Отлично! Теперь создайте ваш первый проект.
   
   Проект — это отдельная работа с уникальными условиями оплаты.
   
   Используйте команду /new_project для создания.
   ```

**Результат:** Пользователь зарегистрирован, готов к созданию проектов.

---

### 5.2. Создание проекта

**Актёр:** Зарегистрированный пользователь

**Шаги:**
1. Пользователь отправляет `/new_project`
2. Бот открывает Telegram Mini App с формой:
   
   **Общая информация:**
   - Название проекта (текст)
   - Описание (текст, опционально)
   
   **Настройка профессии:**
   - Должность (текст)
   - Базовая ставка (целое число, руб, нетто)
   - Базовая ставка переработки (целое число, руб/час)
   - Прогрессивные ставки:
     - [Добавить диапазон]
     - Для каждого: От (часы) → До (часы) → Ставка (руб/ч)
   - Ставка суточных (целое число, руб)
   - Базовое кол-во часов в смене (число)
   - Кол-во часов разрыва между сменами (число)
   - Налоговый процент (число, %)
   - График выплат (выбор из списка)
   - Условия (текстовое поле, для AI)
   
   **Дополнительные услуги:** (опционально)
   - [Добавить услугу]
   - Для каждой:
     - Название
     - Стоимость (руб)
     - Правило применения (радио-кнопки)
     - Ключевые слова (для парсинга)

3. Пользователь заполняет форму и нажимает "Сохранить"
4. Система:
   - Рассчитывает брутто из нетто: `gross = round(net / (1 - tax%))`
   - Сохраняет проект и настройки в БД
   - Делает проект активным
5. Бот отправляет подтверждение:
   ```
   ✅ Проект "Название" успешно создан!
   
   Теперь вы можете вносить смены через чат.
   Просто напишите мне что-то вроде:
   
   "Смена 07:00 до 23:00 + обед + ронин"
   
   Я всё пойму! 😉
   ```

**Результат:** Проект создан, пользователь может вносить смены.

---

### 5.3. Внесение смены через чат

**Актёр:** Пользователь с активным проектом

**Сценарий успешного парсинга:**

1. Пользователь отправляет текстовое сообщение:
   ```
   Смена 07:00 до 23:00 + текущий обед + ронин
   ```

2. Бот отправляет "⏳ Обрабатываю..."

3. Система:
   - Отправляет сообщение в OpenAI API с контекстом:
     - Настройки активного проекта
     - Ключевые слова доп. услуг
     - Текущая дата
   - Получает структурированный JSON с распознанными данными

4. Бот отправляет карточку для подтверждения:
   ```
   📋 Проверьте данные смены:
   
   📅 Дата: 13.01.2026 (сегодня)
   🕐 Начало: 07:00
   🕔 Конец: 23:00
   ⏱ Всего часов: 16 ч
   ⏱ Переработка: 4 ч (сверх 12 ч базы)
   
   Дополнительные услуги:
   ✅ Текущий обед
   ✅ Ронин
   
   [✅ Подтвердить] [✏️ Изменить] [❌ Отменить]
   ```

5. Пользователь нажимает "✅ Подтвердить"

6. Система:
   - Сохраняет смену со статусом "confirmed"
   - Запускает расчёт заработка
   - Обновляет статус на "calculated"

7. Бот отправляет итоговую карточку:
   ```
   💰 Смена рассчитана!
   
   📅 Дата: 13.01.2026
   ⏱ Часов: 16 ч (12 базовых + 4 переработка)
   
   💵 Расчёт:
   • Базовая ставка: 10,000 ₽
   • Переработка (0-2ч @ 500₽): 1,000 ₽
   • Переработка (2-4ч @ 600₽): 1,200 ₽
   • Текущий обед: 500 ₽
   • Ронин: 3,000 ₽
   
   Итого (нетто): 15,700 ₽
   Итого (брутто): 17,253 ₽
   
   [📊 Статистика] [✏️ Изменить]
   ```

**Результат:** Смена внесена, заработок рассчитан.

---

**Сценарий с недостаточными данными:**

1. Пользователь отправляет: `Работал до вечера`

2. Бот отправляет "⏳ Обрабатываю..."

3. AI не может точно определить время начала

4. Бот запрашивает уточнение:
   ```
   🤔 Не смог определить время начала смены.
   
   Во сколько началась смена?
   Например: "07:00" или "в 7 утра"
   ```

5. Пользователь отвечает: `в 7 утра`

6. Система повторяет парсинг с дополнительным контекстом

7. Бот отправляет карточку для подтверждения (как в сценарии выше)

**Результат:** Смена внесена после уточнения.

---

### 5.4. Изменение смены

**Актёр:** Пользователь с подтверждённой сменой

**Шаги:**
1. Пользователь нажимает "✏️ Изменить" в карточке смены
2. Открывается Mini App с формой редактирования:
   - Дата (datepicker)
   - Время начала (timepicker)
   - Время окончания (timepicker)
   - Чекбоксы для доп. услуг
   - Чекбокс "Включить суточные"
3. Пользователь вносит изменения и нажимает "Сохранить"
4. Система:
   - Обновляет данные смены
   - Пересчитывает заработок
5. Бот отправляет обновлённую карточку расчёта

**Результат:** Смена изменена, заработок пересчитан.

---

### 5.5. Просмотр статистики

**Актёр:** Пользователь с несколькими сменами

**Шаги:**
1. Пользователь отправляет `/stats` или нажимает "📊 Статистика"
2. Открывается Mini App со статистикой:
   
   **Фильтры:**
   - Период: Неделя / Месяц / Весь проект
   - Проект (если несколько)
   
   **Показатели:**
   - Всего смен: 12
   - Всего часов: 156 ч
   - Заработок (нетто): 187,400 ₽
   - Заработок (брутто): 205,934 ₽
   - График смен (календарь)
   
   **Список смен:**
   | Дата | Часы | Заработок |
   |------|------|-----------|
   | 13.01 | 16 ч | 15,700 ₽ |
   | 12.01 | 14 ч | 14,200 ₽ |
   | ... | ... | ... |
   
   **Действия:**
   - [📥 Экспорт в CSV]
   - [📄 Экспорт в PDF]

3. Пользователь может:
   - Просматривать детали любой смены
   - Экспортировать данные
   - Фильтровать по периодам

**Результат:** Пользователь видит полную статистику.

---

## 6. Функциональные требования

### 6.1. AI-парсинг сообщений

**Обязательные возможности:**
- ✅ Распознавание времени в различных форматах:
  - "07:00", "7:00", "7 утра", "с семи"
  - "до 23:00", "до вечера", "до одиннадцати"
- ✅ Распознавание даты:
  - Абсолютные: "11.01.2026", "11 января"
  - Относительные: "сегодня", "вчера", "позавчера"
- ✅ Извлечение ключевых слов для доп. услуг:
  - Точное совпадение: "ронин", "обед"
  - Синонимы: "текущий обед" = "обед"
- ✅ Определение проекта:
  - Если не указан → последний активный
  - Если несколько активных → запрос уточнения

**Формат промпта для OpenAI:**
```
Ты — парсер сообщений для учёта рабочих смен.

Контекст:
- Текущая дата: {current_date}
- Активный проект: {project_name}
- Базовое количество часов: {base_hours}
- Доступные услуги: {services_keywords}

Сообщение пользователя:
"{user_message}"

Верни JSON:
{
  "date": "YYYY-MM-DD",
  "start_time": "HH:MM",
  "end_time": "HH:MM",
  "services": ["service_name_1", "service_name_2"],
  "confidence": 0.95,
  "missing_fields": []
}

Если не можешь определить поле — добавь его в "missing_fields".
```

**Обработка результата:**
- `confidence > 0.8` → показываем карточку подтверждения
- `confidence < 0.8` или `missing_fields` → запрашиваем уточнение
- Ошибка API → предлагаем ручной ввод через Mini App

---

### 6.2. Расчёт заработка

**Алгоритм расчёта:**

```python
# 1. Базовая оплата
if total_hours <= base_shift_hours:
    base_pay_net = base_rate_net
    base_pay_gross = base_rate_gross
    overtime_hours = 0
else:
    base_pay_net = base_rate_net
    base_pay_gross = base_rate_gross
    overtime_hours = total_hours - base_shift_hours

# 2. Переработки (прогрессивные ставки)
overtime_pay = 0
remaining_hours = overtime_hours

for rate in progressive_rates (ordered):
    hours_in_bracket = min(remaining_hours, rate.hours_to - rate.hours_from)
    overtime_pay += hours_in_bracket * rate.rate
    remaining_hours -= hours_in_bracket
    
    if remaining_hours <= 0:
        break

# 3. Суточные (только если установлен флаг is_expense_day)
if shift.is_expense_day:
    daily_allowance_pay = daily_allowance
else:
    daily_allowance_pay = 0

# 4. Дополнительные услуги
services_pay = 0
for service in shift.services:
    if service.application_rule == "every_shift" or service in shift.mentioned_services:
        services_pay += service.cost

# 5. Итого
total_net = base_pay_net + overtime_pay + daily_allowance_pay + services_pay
total_gross = round(total_net / (1 - tax_percentage / 100))
```

**Специальные условия (из поля "conditions"):**
- Обрабатываются через AI на этапе расчёта
- Примеры:
  - "7-й рабочий день подряд × 2" → проверяем последовательность
  - "Ночная смена (22:00-06:00) +20%" → проверяем время
  - "Тревел дэй = 0.5 смены" → применяем коэффициент

**Формат промпта для применения условий:**
```
Примени специальные условия к расчёту смены.

Условия:
"{profession.conditions}"

Данные смены:
- Дата: {shift.date}
- Время: {shift.start_time} - {shift.end_time}
- Последовательность рабочих дней: {consecutive_days}

Текущий расчёт:
- Базовая оплата: {base_pay_net}
- Переработки: {overtime_pay}

Верни JSON с корректировками:
{
  "base_pay_multiplier": 1.0,
  "overtime_pay_multiplier": 1.0,
  "additional_pay": 0,
  "reason": "Описание применённых условий"
}
```

---

### 6.3. Обработка крайних случаев

**1. Переход смены через полночь:**
```
Начало: 23:00 (день 1)
Конец: 07:00 (день 2)
Итого часов: 8 ч
```
- Система корректно определяет дату окончания как следующий день
- Учитывает при расчёте ночных условий (если есть)

**2. Конфликт смен:**
```
Существующая смена: 07:00-19:00 (13.01.2026)
Новая смена: 15:00-23:00 (13.01.2026)
```
- Бот предупреждает о пересечении
- Предлагает:
  - Изменить время новой смены
  - Отменить старую смену
  - Продолжить с перекрытием (риск)

**3. Множественные активные проекты:**
```
Проекты:
1. Фильм "А" (активен)
2. Фильм "Б" (активен)
```
- Бот спрашивает: "К какому проекту относится смена?"
- Кнопки: [Фильм "А"] [Фильм "Б"]
- Можно установить проект по умолчанию в настройках

**4. Пересчёт при изменении тарифов:**
- Существующие смены НЕ пересчитываются автоматически
- В карточке смены появляется предупреждение:
  ```
  ⚠️ Тарифы проекта изменены после расчёта этой смены.
  
  [🔄 Пересчитать с новыми тарифами]
  ```

**5. 7-й рабочий день подряд:**
- Система автоматически отслеживает последовательность
- Учитывает поле "часы разрыва" для определения непрерывности:
  - Если между сменами < X часов → считаем непрерывной работой
  - Если ≥ X часов → сбрасываем счётчик

---

## 7. Нефункциональные требования

### 7.1. Производительность
- Парсинг сообщения: < 3 секунд
- Расчёт заработка: < 1 секунды
- Загрузка Mini App: < 2 секунд

### 7.2. Надёжность
- База данных: автоматический бэкап раз в день
- Логирование всех действий пользователя
- Обработка ошибок API с fallback на ручной ввод

### 7.3. Безопасность
- API ключи хранятся в переменных окружения
- Нет доступа к данным других пользователей
- SQLite с правильными правами доступа (chmod 600)

### 7.4. Масштабируемость
- Текущая версия: до 100 пользователей на одной SQLite базе
- Возможность миграции на PostgreSQL в будущем

---

## 8. Ограничения MVP

**В первой версии НЕ реализуется:**
- ❌ Тип контрагента "Транспорт"
- ❌ Экспорт в PDF (только CSV)
- ❌ Календарь смен (только список)
- ❌ Прогноз выплат
- ❌ Уведомления о выплатах
- ❌ Работа с несколькими валютами
- ❌ Интеграция с бухгалтерскими системами
- ❌ Мультиязычность (только русский)

**Будет реализовано позже:**
- 🔜 Тип "Транспорт" (v1.1)
- 🔜 Экспорт в PDF (v1.2)
- 🔜 Календарь и графики (v1.3)

---

## 9. Глоссарий

| Термин | Описание |
|--------|----------|
| Контрагент | Фрилансер или подрядчик, ведущий учёт смен |
| Смена | Период работы с началом и окончанием |
| Переработка | Часы сверх базового количества часов |
| Прогрессивная ставка | Разные тарифы для разных диапазонов переработки |
| Суточные | Фиксированная выплата за командировку (start.exp) |
| Доп. услуги | Дополнительные платные услуги (аренда, обслуживание и т.д.) |
| Нетто | Сумма "чистыми", которую получит контрагент |
| Брутто | Сумма с учётом налога (до вычета налога) |
| Mini App | Веб-приложение внутри Telegram |

---

## 10. Приложения

### 10.1. Примеры пользовательских сообщений

**Простые:**
- "Смена 7-19"
- "Работал с 9 до 18"
- "07:00 - 23:00"

**С датой:**
- "Вчера работал 8-20"
- "11 января 07:00 до 19:00"
- "Позавчера смена 7-23"

**С услугами:**
- "Смена 7-23 + обед + ронин"
- "07:00-19:00 текущий обед"
- "Работал 8-20 с рониным и обедом"

**Сложные:**
- "Вчера работал с 7 утра до 11 вечера с текущим обедом и рониным"
- "11 января: 07:00 - 23:00 + текущий обед + обслуживание ронина"

### 10.2. Пример JSON от OpenAI

```json
{
  "date": "2026-01-13",
  "start_time": "07:00",
  "end_time": "23:00",
  "services": ["текущий обед", "ронин"],
  "confidence": 0.95,
  "missing_fields": []
}
```

### 10.3. Пример детального расчёта

```json
{
  "shift_id": 123,
  "date": "2026-01-13",
  "total_hours": 16,
  "base_hours": 12,
  "overtime_hours": 4,
  "breakdown": {
    "base_pay": {
      "net": 10000,
      "gross": 10989
    },
    "overtime": [
      {
        "bracket": "0-2ч",
        "hours": 2,
        "rate": 500,
        "total": 1000
      },
      {
        "bracket": "2-4ч",
        "hours": 2,
        "rate": 600,
        "total": 1200
      }
    ],
    "daily_allowance": 0,
    "services": [
      {
        "name": "Текущий обед",
        "cost": 500
      },
      {
        "name": "Ронин",
        "cost": 3000
      }
    ]
  },
  "total": {
    "net": 15700,
    "gross": 17253
  }
}
```

---
