# Следующие шаги

## 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА (Фаза 4)

**Текущий блокер:** Данные из Mini App не доходят до бота

### Проблема:
1. При нажатии кнопки "Сохранить проект" в Mini App:
   - `tg.sendData(JSON.stringify(data))` вызывается ✅
   - `tg.close()` закрывает Mini App ✅
2. НО обработчик `handle_web_app_data` в боте НЕ срабатывает ❌
3. В логах бота нет записей о получении данных ❌
4. Проект не создаётся в БД ❌

### Возможные причины:
- Проблема с фильтром `lambda message: message.web_app_data`
- Несовместимость версии aiogram с Web App
- Telegram не принимает данные от Mini App
- Нужна регистрация Mini App через BotFather

### План действий на завтра:
1. **Проверить версию aiogram:**
```bash
   pip show aiogram
```
   Возможно нужно обновить до последней версии

2. **Попробовать другой фильтр:**
```python
   from aiogram import F
   
   @router.message(F.web_app_data)
   async def handle_web_app_data(message: Message):
       ...
```

3. **Добавить catch-all обработчик:**
```python
   @router.message()
   async def catch_all(message: Message):
       print(f"Получено сообщение: {message}")
       print(f"Есть web_app_data: {hasattr(message, 'web_app_data')}")
```

4. **Проверить документацию aiogram 3.x** по работе с Web Apps

5. **Тестовый вариант:** Попробовать отправить данные через обычное текстовое сообщение вместо `web_app_data`

---

## После решения проблемы:

### Шаг 4.8: Сохранение профессии в БД
- Доделать обработку `action: 'add_profession'` в боте
- Сохранить профессию через `create_profession()`
- Сохранить прогрессивные ставки через `add_progressive_rate()`
- Отправить подтверждение пользователю

### Шаг 4.9: Форма добавления услуги
- Создать `add-service.html` и `add-service.js`
- Поля: название, стоимость, налог, ключевые слова
- 4 варианта условий применения:
  1. Каждая смена
  2. Каждая смена профессии (выбор профессии)
  3. При упоминании
  4. Вместе с другой услугой (выбор услуги)

### Шаг 4.10: Главный экран (список проектов)
- Создать `index.html` со списком проектов
- Показывать карточки проектов с заработком
- Кнопка "✕ Закрыть проект" (архивация)
- Кнопка "+ Создать новый проект"
- Загрузка данных с сервера через API (или через бота)

**Общее время до завершения Фазы 4:** ~2-3 дня (после решения блокера)