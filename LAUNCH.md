# 🚀 Как запустить бота

**Важно:** Нужно 3 терминала одновременно!

---

## ТЕРМИНАЛ 1: Flask API сервер

```bash
cd ~/projects/bots/yomi_profit_test
source venv/bin/activate
python api_server.py
```

✅ Должно появиться:
```
🚀 API сервер запущен на http://localhost:8001
```

**Оставь этот терминал открытым!**

---

## ТЕРМИНАЛ 2: Ngrok

```bash
ngrok http 8001
```

✅ Должно появиться:
```
Forwarding  https://abc123xyz.ngrok-free.app -> http://localhost:8001
```

📝 **СКОПИРУЙ URL!** (например: `https://abc123xyz.ngrok-free.app`)

**Оставь этот терминал открытым!**

---

## ТЕРМИНАЛ 3: Telegram бот

### Шаг 1: Обнови URL

```bash
cd ~/projects/bots/yomi_profit_test
nano handlers/miniapp.py
```

Найди строку ~13 и вставь **свой ngrok URL**:

```python
webapp_url = f"https://ТВОЙ-URL.ngrok-free.app/index.html?user_id={message.from_user.id}"
```

Сохрани: `Ctrl+O` → `Enter` → `Ctrl+X`

### Шаг 2: Запусти бота

```bash
python bot.py
```

✅ Должно появиться:
```
INFO - Run polling for bot
```

**Готово! Бот работает!**

---

## 🧪 Тестируй:

1. Открой бота в Telegram
2. Отправь `/projects`
3. Нажми кнопку "📋 Мои проекты"
4. Создай проект → добавь профессию → добавь услугу
5. Отправь боту: `"Смена 07:00 до 19:00 + обед"`
6. Подтверди смену → получи расчёт!

---

## 🛑 Как остановить:

Нажми `Ctrl+C` в каждом из 3 терминалов

---

## ⚠️ Важно!

**Каждый раз при перезапуске ngrok URL меняется!**

Нужно:
1. Скопировать новый URL из терминала 2
2. Обновить `handlers/miniapp.py` (терминал 3, шаг 1)
3. Перезапустить бота (`Ctrl+C`, потом `python bot.py`)

---

## Чеклист:

- [ ] Терминал 1: `python api_server.py` ✅
- [ ] Терминал 2: `ngrok http 8001` ✅
- [ ] Скопировал ngrok URL ✅
- [ ] Обновил `handlers/miniapp.py` ✅
- [ ] Терминал 3: `python bot.py` ✅
- [ ] Протестировал `/projects` ✅

---

Сохрани эту инструкцию! 📌